[project]
name = "engdept"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.31",
  "pandas>=2.1",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
